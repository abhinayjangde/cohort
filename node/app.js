const {substract, addition,pi} = require('./math')
// import {sub,add,pi} from "./math"

// console.log(sum(1,2))
console.log(addition(1,2)) // in js everything is an object.
console.log(substract(1,2)) // in js everything is an object.
console.log(pi) // in js everything is an object.
